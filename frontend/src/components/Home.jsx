import React from 'react';

function Home() {
  const headingStyle = {
    color: '#ff5733', // bold color
    textAlign: 'center',
    fontWeight: 'bold'
  };

  return <h1 style={headingStyle}>Welcome Home</h1>;
}

export default Home;
